version: "3.8"
services:
  my-mongo-db:
    container_name: "my-mongo-db"
    image: mongo:4.4.3
    ports:
      - 27017:27017
    expose:
      - 27017
    networks:
        frontend:
            ipv4_address: 173.80.0.2
            
  library:
    container_name: "api_services"
    image: spring:0.0.1-SNAPSHOT
      - 8080:8080
    depends_on:
      - my-mongo-db
    networks:
        frontend:
            ipv4_address: 173.80.0.3

  nginx:
    container_name: "nginx"
    image: nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
        frontend:
            ipv4_address: 173.80.0.5

networks:
 
  frontend:
    ipam:
      config:
        - subnet: 173.80.0.0/24